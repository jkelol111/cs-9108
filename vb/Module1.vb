Module Module1
    ' Write in VB.Net code to declare a variable
    ' to store the age of a person
    Sub Main()
        Dim Age As Integer
        Console.WriteLine("Input your age: ")
        age = Console.ReadLine()
        Console.WriteLine("You are " & age & " years old.")
        Console.ReadLine()
    End Sub
End Module
